# import requests
# import yaml

# # Make a GET request to an endpoint in your Django application
# response = requests.get("http://localhost:8000/schema/")

# # Convert the response to a Python dict
# schema = response.json()

# # Dump the dict to a YAML file
# with open("schema.yml", "w") as file:
#     yaml.dump(schema, file)
